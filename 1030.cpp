#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

void josephus() {
    int h, salto;
    cin >> h >> salto;
    vector homens(h);
    iota(homens.begin(), homens.end(), 1);
    while(homens.size() > 0)
    {
        
    }
}

int main() {
    int t;
    for (int i = 0; i < t; i++) {

    }
    return 0;
}